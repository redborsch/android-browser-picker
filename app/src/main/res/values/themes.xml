<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BrowserPicker" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BrowserPicker" parent="Base.Theme.BrowserPicker" />

    <style name="Theme.BrowserPicker.Transparent" parent="Base.Theme.BrowserPicker" >
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowActivityTransitions">false</item>
        <item name="android:windowEnterTransition">@null</item>
        <item name="bottomSheetDialogTheme">@style/BrowserBottomSheetDialog</item>
    </style>

    <style name="BrowserBottomSheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BrowserBottomSheet</item>
    </style>

    <style name="BrowserBottomSheet" parent="Widget.Design.BottomSheet.Modal">
        <item name="behavior_peekHeight">256dp</item>
        <item name="android:maxWidth">640dp</item>
    </style>
</resources>