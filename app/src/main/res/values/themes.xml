<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.BrowserPicker" parent="Theme.Material3Expressive.DayNight.NoActionBar">
        <!-- Transparent navigation bars for edge-to-edge.
         Otherwise, navigation bar will have an ugly black background. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>

    <style name="Theme.BrowserPicker" parent="Base.Theme.BrowserPicker" />

    <style name="Theme.BrowserPicker.Transparent" parent="Base.Theme.BrowserPicker">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
        <item name="android:windowActivityTransitions">false</item>
        <item name="android:windowEnterTransition">@null</item>
        <item name="bottomSheetDialogTheme">@style/BrowserBottomSheetDialog</item>
    </style>

    <style name="BrowserBottomSheetDialog" parent="ThemeOverlay.Material3.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BrowserBottomSheet</item>
    </style>

    <style name="BrowserBottomSheet" parent="Widget.Material3.BottomSheet.Modal">
        <item name="behavior_peekHeight">256dp</item>

        <!-- Real edge-to-edge support, also cutouts are not handled properly anyway.
         Note that we still enable top padding in order for bottom sheet to fully expand
         (even though without real edge-to-edge support, but good enough for now as the fix
          would require currently patching BottomSheetDialog.EdgeToEdgeCallback). -->
        <item name="marginLeftSystemWindowInsets">false</item>
        <item name="marginRightSystemWindowInsets">false</item>
        <item name="marginTopSystemWindowInsets">false</item>
        <item name="marginBottomSystemWindowInsets">false</item>
        <item name="paddingBottomSystemWindowInsets">false</item>
        <item name="paddingTopSystemWindowInsets">true</item>
        <item name="paddingLeftSystemWindowInsets">false</item>
        <item name="paddingRightSystemWindowInsets">false</item>

        <!-- For testing -->
<!--        <item name="android:maxWidth">1000dp</item>-->
    </style>
</resources>